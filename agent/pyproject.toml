[project]
name = "search-agent"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
  "google-genai>=0.6.0",
  "langgraph>=0.2.0",
  "python-dotenv>=1.0.1",
  "pydantic>=2.6",
  "transformers>=4.40.0",
]

[tool.setuptools.packages.find]
include = ["nodes*", "llm*", "tools*", "scripts*", "safety*"]
